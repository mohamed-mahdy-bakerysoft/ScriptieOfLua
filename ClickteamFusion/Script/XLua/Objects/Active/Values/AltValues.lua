function Values(id)
    local object = mmf.newObject(id)

	-- Index
	local A = object.values[1]
	-- MMF alphabetical letter
	local B = object.values[mmf.ALT_B]
	-- Name
	local C = object.values['Value C']
end